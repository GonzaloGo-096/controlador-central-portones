    # MQTT Broker - HiveMQ Cloud (obligatorio)
    # Usar mqtts:// para TLS (puerto 8883)
    MQTT_BROKER_URL=mqtts://b95a357c965747e1904adf9c0d54ce14.s1.eu.hivemq.cloud:8883

    # MQTT Auth - HiveMQ Cloud (obligatorio para broker real)
    MQTT_USERNAME=controlador-portones
    MQTT_PASSWORD=tu_contraseña_aqui

    # MQTT Cliente (opcional)
    MQTT_CLIENT_ID=controlador-local

    # HTTP API (opcional, default 3000)
    PORT=3000

    # Test: publicar mensaje de prueba al conectar (opcional, true/false)
    MQTT_TEST_ON_CONNECT=true

    # Telegram tenants: fake para debug sin DB (true/false)
    USE_FAKE_TENANTS=true
    TELEGRAM_BOT_INTERNAL_SECRET=definir_un_secreto_largo_y_unico

    # PostgreSQL (usar solo DATABASE_URL)
    DATABASE_URL=postgresql://postgres:tu_password@localhost:5432/portones_db
    REDIS_URL=redis://localhost:6379
